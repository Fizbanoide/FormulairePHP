<h1>Liste des utilisateurs</h1>
<form method="get" name="formulaire" action="{{ url('users.new') }}">
  <button type="submit">Ajouter</button>
</form>
<table>
<thead>
  <tr>
    <th>ID</th>
    <th>NOM</th>
    <th>PRENOM</th>
    <th>ARRET MAISON</th>
    <th>ARRET TRAVAIL</th>
    <th>ACTION</th>
  </tr>
</thead>
<tbdoy>
{% for user in users %}
   <tr>
     <td>{{user.id}}</td>
     <td>{{user.nom}}</td>
     <td>{{user.prenom}}</td>
     <td>{{user.arretMaison}}</td>
     <td>{{user.arretTravail}}</td>
     <td>
       <form method="post" name="formulaire" action="{{ url('users.delete', {id: user.id}) }}">
            <button type="submit">Supprimer</button>
        </form>
      </td>
      <td>
        <form method="get" name="formulaire" action="{{ url('users.edit', {id: user.id}) }}">
          <button type="submit">Modifier</button>
        </form>
      </td>
   </tr>
{% endfor %}
</tbdoy>
</table>


<form method="post" name="authentification" action="{{ url('users.auth') }}">
  <label for="name">Nom : </label>
  <input type="text" name="lastname" />

  <label for="name">Prénom : </label>
  <input type="text" name="firstname" />

  <button type="submit">Authentification</button>


</form>
